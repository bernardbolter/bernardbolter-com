import type { Payload } from 'payload'

/**
 * Stub: when enrichment is implemented, write matching `mergeCandidates` on both records.
 * Safe no-op for now.
 */
export async function proposeMerge(_payload: Payload): Promise<void> {
  return
}
