/**
 * Background enrichment agent (stub). Future: match externalIdentifiers, name+city,
 * write mergeCandidates with status pending.
 */
export async function runEnrichmentAgent(): Promise<void> {
  return
}
